{
  "name": "shakes-plus-plus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "spp": "npm run build && node dist/main.js",
    "global": "npm run build && npm install -g .  && npm link && node add-shebang.js "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.1.0",
    "@types/prompt-sync": "^4.2.3",
    "@types/readline-sync": "^1.4.8"
  },
  "dependencies": {
    "prompt-sync": "^4.2.0",
    "readline-sync": "^1.4.10"
  },
  "bin": {
    "spp": "./dist/main.js"
  }
}
